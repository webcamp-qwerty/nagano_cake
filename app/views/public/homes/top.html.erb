<div class="container">
  <div class="row">
    <div class="col-lg-3 my-3">
      <table class="table table-borderd">
        <thead>
          <tr>
            <th>ジャンル検索</th>
          </tr>
        </thead>
        <tbody>
          <% @genres.each do |genre| %>
            <tr>
              <td>
                <%= link_to genre.name, search_path('search[value]': genre.id, 'search[how]': "match", 'search[name]': genre.name ) %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="col-lg-9">
      <div class="card mt-10px my-3">
        <div class="card-img hgiht">
          <%= image_tag 'top_image_2.jpeg',:size =>'830x400' %>
        </div>
        <div class="card-img-overlay d-flex">
          <p class="text-dark h4">
            ようこそ、ながのケーキへ！<br>
            このサイトは、ケーキ販売のECサイトになります。<br>
            会員でない方も商品閲覧はできますが、<br>
            購入には会員登録が必要になります。<br>
          </p>
        </div>
      </div>

      <h1>新着商品</h1>
        <div class="top-yoko">
          <% @items.each do |item| %>
            <div class="tate">
              <p>

                <%= link_to item_path(item) do %>
                  <%= attachment_image_tag item, :image, :fill, 200, 100, format: 'jpeg', fallback: "no_image.png" %>
                <% end %>
              </p>
              <p><%= item.name %></p>
              <p>￥<%= item.add_tax_price.to_s(:delimited) %></p>
            </div>
          <% end %>
        </div>
      <div class="text-right">
        <%= link_to "全ての商品を見る＞", items_path %>
      </div>
    </div>
  </div>
</div>
